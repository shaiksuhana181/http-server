# Development of a http server
A lightweight HTTP server developed using Python.It runs locally on a system. This server listens to client requests and process them efficiently.Any submitted data is stored in a text file.
